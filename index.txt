<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bad Peach – Calories & Protein Calculator</title>
  <meta name="description" content="Quick calories and protein calculator for Bad Peach clients. Uses Laura’s book formulas and brand.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Anton:wght@400;700&display=swap" rel="stylesheet">
  <style>
    :root { --bg:#fffefc; --text:#191919; --muted:#5a5a5a; --card:#ffffff; --line:#ece9e4; --accent:#ebcac2; }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{margin:0;background:var(--bg);color:var(--text);font:16px/1.5 'Times New Roman', Times, serif}
    .wrap{max-width:920px;margin:0 auto;padding:0 16px}
    header{position:sticky;top:0;z-index:50;background:var(--bg);border-bottom:1px solid var(--line);backdrop-filter:saturate(140%) blur(4px)}
    .head{display:flex;align-items:center;justify-content:space-between;padding:10px 0}
    .brand{display:flex;align-items:center;gap:10px}
    .brand .dot{width:12px;height:12px;border-radius:3px;background:var(--accent)}
    .title{font-family:'Anton',system-ui,sans-serif;font-size:32px;letter-spacing:.3px;margin:18px 0 6px}
    .subtitle{color:var(--muted);margin:0 0 20px}
    .grid{display:grid;grid-template-columns:1fr;gap:16px}
    @media(min-width:900px){.grid{grid-template-columns:1fr 1fr}}
    .card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:16px;box-shadow:0 1px 0 rgba(25,25,25,.03)}
    label{display:block;font-weight:700;margin-bottom:6px;font-family:'Anton',system-ui,sans-serif;letter-spacing:.2px}
    input,select{width:100%;padding:12px;border-radius:12px;border:1px solid var(--line);background:#fff;color:var(--text)}
    .row{display:grid;grid-template-columns:1fr 1fr;gap:12px}
    .help{color:var(--muted);font-size:13px;margin-top:6px}
    .btnbar{display:flex;gap:10px;flex-wrap:wrap;margin-top:8px}
    button{background:var(--accent);border:0;color:var(--text);font-weight:800;padding:12px 16px;border-radius:12px;cursor:pointer}
    button.secondary{background:#f6f3ef}
    .kpi{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
    .item{background:#fff;border:1px solid var(--line);border-radius:14px;padding:14px}
    .label{color:var(--muted);font-size:12px}
    .value{font-size:20px;font-weight:800;margin-top:6px}
    .hr{height:1px;background:var(--line);margin:16px 0}
    .note{font-size:13px;color:var(--muted)}
    footer{margin-top:24px;border-top:1px solid var(--line);background:#fff}
    .foot{display:flex;flex-wrap:wrap;gap:10px;align-items:center;justify-content:space-between;padding:14px 0;color:var(--muted);font-size:12px}
    a{color:var(--text);text-decoration:none}

    /* CTA section */
    .divider{height:1px;background:var(--line);margin:40px 0 24px}
    .cta{background:var(--bg);color:var(--text);text-align:center;border-radius:16px;padding:28px 16px}
    .cta .cover{max-width:320px;width:100%;border-radius:12px;margin:0 auto 16px;display:block}
    .cta h2{font-family:'Anton',system-ui,sans-serif;letter-spacing:.3px;margin:0 0 10px}
    .cta p{margin:0 auto 12px;max-width:680px}
    .cta .btns{display:flex;flex-wrap:wrap;gap:12px;justify-content:center;margin-top:16px}
    .btn{background:var(--accent);color:var(--text);border:2px solid var(--accent);padding:14px 28px;border-radius:12px;cursor:pointer;font-family:'Anton',system-ui,sans-serif;font-size:16px;transition:all .4s ease}
    .btn.secondary{background:transparent;color:var(--text);border-color:var(--text)}
    .btn:hover{background:var(--text);color:var(--bg);border-color:var(--text)}
    .btn.secondary:hover{background:var(--accent);color:var(--text);border-color:var(--accent)}
    @media(min-width:900px){.cta{padding:40px 24px}.cta .btn{font-size:17px;padding:16px 36px}}

    /* Instagram CTA */
    .cta-insta{background:var(--accent);color:var(--text);text-align:center;border-radius:16px;padding:28px 16px;margin-top:40px}
    .cta-insta h2{font-family:'Anton',system-ui,sans-serif;letter-spacing:.3px;margin:0 0 10px}
    .cta-insta p{margin:0 auto 12px;max-width:680px}
    .cta-insta .btns{display:flex;flex-wrap:wrap;gap:12px;justify-content:center;margin-top:16px;align-items:center}
    .cta-insta .btn{background:#fffefc;color:var(--text);border:2px solid var(--text);padding:14px 28px;border-radius:12px;cursor:pointer;font-family:'Anton',system-ui,sans-serif;font-size:16px;transition:all .4s ease;text-decoration:none;display:flex;align-items:center;gap:10px}
    .cta-insta .btn:hover{background:var(--text);color:#fffefc;border-color:var(--text)}
  </style>
</head>
<body>
  <div class="divider"></div>
  <section class="cta" aria-label="Call to action: Becoming Your Fittest Forever">
    <img class="cover" src="cover.png" alt="Becoming Your Fittest Forever book cover" />
    <h2>Next Step: Turn Your Numbers Into Results</h2>
    <p>You’ve got your calorie and protein targets. Now follow the system that makes them work for you.</p>
    <p><strong><a href="https://badpeachfitness.store/shop/3d377cf8-b293-42ba-91f2-593086a5a741">Becoming Your Fittest Forever</a></strong> is a 185+ page blueprint with lifetime access to your 7-day meal-plan generator, strength training programs, and step-by-step guidance for fat loss, maintenance, or muscle gain.</p>
    <div class="btns">
      <a class="btn" href="https://badpeachfitness.store/shop/3d377cf8-b293-42ba-91f2-593086a5a741">GET THE BLUEPRINT</a>
      <a class="btn secondary" href="https://badpeachfitness.store/shop/3d377cf8-b293-42ba-91f2-593086a5a741">VIEW BOOK DETAILS</a>
    </div>
  </section>

  <div class="divider"></div>
  <section class="cta-insta" aria-label="Find me on Instagram">
    <h2>Come Hang Out on Instagram</h2>
    <p>For fitness tips and behind-the-scenes training.</p>
    <div class="btns">
      <a class="btn" href="https://instagram.com/bad.peach.fitness" target="_blank">
        <svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' width='18' height='18' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect x='2' y='2' width='20' height='20' rx='5' ry='5'/><path d='M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z'/><line x1='17.5' y1='6.5' x2='17.51' y2='6.5'/></svg>
        @bad.peach.fitness
      </a>
    </div>
  </section>
  
  <!-- Standalone Hero CTA for ebook sales page -->
  <section class="hero-cta" aria-label="Becoming Your Fittest Forever – Hero">
    <div class="hero-wrap">
      <div class="hero-media">
        <!-- Replace with your actual cover file path -->
        <img src="cover.png" alt="Becoming Your Fittest Forever book cover" />
      </div>
      <div class="hero-copy">
        <h1>Becoming Your Fittest Forever</h1>
        <p class="tag">A 185+ page fitness blueprint that helps you eat, train, and live for sustainable results.</p>
        <ul class="points">
          <li>Lifetime access to your 7‑day meal‑plan generator</li>
          <li>Strength training programs for home and gym</li>
          <li>Clear paths for fat loss, maintenance, or muscle gain</li>
        </ul>
        <div class="btns">
          <a class="btn primary" href="https://badpeachfitness.store/shop/3d377cf8-b293-42ba-91f2-593086a5a741">GET THE BLUEPRINT</a>
          <a class="btn outline" href="https://badpeachfitness.store/shop/3d377cf8-b293-42ba-91f2-593086a5a741">VIEW DETAILS</a>
        </div>
      </div>
    </div>
  </section>

  <style>
    /* ===== Hero CTA (standalone, drop-in) ===== */
    .hero-cta{background:var(--bg);color:var(--text);padding:32px 16px}
    .hero-cta .hero-wrap{max-width:1100px;margin:0 auto;display:grid;grid-template-columns:1fr;gap:20px;align-items:center}
    .hero-cta .hero-media img{width:100%;max-width:520px;border-radius:16px;display:block;margin:0 auto}
    .hero-cta .hero-copy{max-width:600px;margin:0 auto;text-align:center}
    .hero-cta h1{font-family:'Anton',system-ui,sans-serif;font-size:38px;letter-spacing:.3px;margin:0 0 8px}
    .hero-cta .tag{color:var(--muted);margin:0 0 14px}
    .hero-cta .points{list-style:none;padding:0;margin:0 0 18px}
    .hero-cta .points li{margin:6px 0}
    .hero-cta .btns{display:flex;flex-wrap:wrap;gap:12px;justify-content:center}
    .hero-cta .btn{font-family:'Anton',system-ui,sans-serif;display:inline-block;padding:16px 28px;border-radius:12px;text-decoration:none;transition:all .35s ease}
    .hero-cta .btn.primary{background:var(--accent);color:var(--text);border:2px solid var(--accent)}
    .hero-cta .btn.outline{background:transparent;color:var(--text);border:2px solid var(--text)}
    .hero-cta .btn.primary:hover{background:var(--text);color:var(--bg);border-color:var(--text)}
    .hero-cta .btn.outline:hover{background:var(--accent);color:var(--text);border-color:var(--accent)}
    @media(min-width:980px){
      .hero-cta{padding:48px 20px}
      .hero-cta .hero-wrap{grid-template-columns:520px 1fr;gap:32px}
      .hero-cta .hero-copy{text-align:left;margin:0}
      .hero-cta h1{font-size:44px}
    }
  </style>
</body>
</html>
